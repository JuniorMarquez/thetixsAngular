import { FiltroDirective } from './filtro.directive';

describe('FiltroDirective', () => {
  it('should create an instance', () => {
    const directive = new FiltroDirective();
    expect(directive).toBeTruthy();
  });
});
